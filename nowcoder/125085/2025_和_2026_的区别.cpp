#include <iostream>
int main()
{
     int t = '<';
     int tt = '\n';
     std::cout << t / tt << ".";
     std::cout << t / tt;
     std::cout << t / tt;
     std::cout << t / tt;
     std::cout << t / tt;
     std::cout << t / tt;
}